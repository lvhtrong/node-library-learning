{
  "name": "testing-next",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/testing-next/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "cd apps/testing-next && npx firebase deploy --project testing-react-router-5fbda --only hosting:testing-next",
            "forwardAllArgs": false
          }
        ],
        "parallel": false
      }
    }
  }
}
